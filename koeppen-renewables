#!/usr/bin/env python3
"""
Main executable for the Koeppen renewables processing pipeline.

This script:
- parses CLI arguments
- determines the spatial domain (global or user-specified)
- iterates over 20×20° tiles
- runs:
    1) atlas & settlement resampling
    2) ERA5 variability calculations

This is the ONLY entry point users should run.
"""

#!/usr/bin/env python3
import sys
import os
from pathlib import Path
import argparse
import xarray as xr

REPO_ROOT = Path(__file__).parent
sys.path.extend([str(REPO_ROOT), str(REPO_ROOT / "src")])

from src.abundance_atlas import resample_atlas
from src.process_tiles import generate_tiles, run_variability_for_tile
from config import GLOBAL_DOMAIN, TILE_SIZE, REFERENCE_RESOLUTION, PATHS, START_YEAR, END_YEAR

def parse_args():
    parser = argparse.ArgumentParser(description="Koeppen pipeline")
    parser.add_argument("--global", action="store_true", help="Full domain")
    parser.add_argument("--bounds", nargs=4, type=float, metavar=("MINX", "MINY", "MAXX", "MAXY"))
    return parser.parse_args()

def main():
    args = parse_args()
    domain = dict(zip(['minx', 'miny', 'maxx', 'maxy'], args.bounds)) if args.bounds else GLOBAL_DOMAIN
    
    output_dir = REPO_ROOT / "results" / "automatic"
    output_dir.mkdir(parents=True, exist_ok=True)

    tiles = generate_tiles(domain["minx"], domain["miny"], domain["maxx"], domain["maxy"], TILE_SIZE)

    for tile in tiles:
        # Simplified naming: minx_miny_maxx_maxy
        tile_str = "_".join(map(str, tile))
        out_file = output_dir / f"processed_{tile_str}_{START_YEAR}_{END_YEAR}.nc"

        if out_file.exists():
            print(f"Tile {tile_str} exists. Skipping.")
            continue

        print(f"\n--- Processing Tile: {tile_str} ---")
        try:
            # 1. Atlas
            print("  -> Resampling Atlas...")
            ds_main = resample_atlas(tile, PATHS, resolution=REFERENCE_RESOLUTION)

            # 2. Variability (Wind & Solar)
            for label, var in {"solar": "ssrd", "wind": "ws100"}.items():
                print(f"  -> Computing {label} variability...")
                ds_var = run_variability_for_tile(tile, var)
                # Merge into the main dataset for this tile
                ds_main = xr.merge([ds_main, ds_var.rename({v: f"{label}_{v}" for v in ds_var.data_vars})])

            # 3. Atomic Save (HPC Safe)
            tmp_path = str(out_file) + ".tmp"
            ds_main.to_netcdf(tmp_path)
            os.rename(tmp_path, out_file)
            print(f"  [SUCCESS] Saved to {out_file.name}")

        except Exception as e:
            print(f"  [ERROR] Tile {tile_str} failed: {e}")
            if 'tmp_path' in locals() and os.path.exists(tmp_path):
                os.remove(tmp_path)

if __name__ == "__main__":
    main()

# import sys
# from pathlib import Path
# import argparse

# # Allow imports from src/ and repo root
# REPO_ROOT = Path(__file__).parent
# sys.path.append(str(REPO_ROOT))
# sys.path.append(str(REPO_ROOT / "src"))

# from src.abundance_atlas import resample_atlas
# from src.process_tiles import generate_tiles, run_variability_for_tile
# from config import (
#     GLOBAL_DOMAIN,
#     TILE_SIZE,
#     REFERENCE_RESOLUTION,
#     PATHS,
#     START_YEAR,
#     END_YEAR,
# )


# def parse_args():
#     """
#     Parse command-line arguments.

#     --global : process full predefined global domain (default)
#     --bounds : process only a user-specified bounding box
#     """
#     parser = argparse.ArgumentParser(
#         description="Koeppen renewable energy potential pipeline"
#     )

#     parser.add_argument(
#         "--global",
#         dest="run_global",
#         action="store_true",
#         help="Process the full global domain (default)",
#     )

#     parser.add_argument(
#         "--bounds",
#         nargs=4,
#         type=float,
#         metavar=("MINX", "MINY", "MAXX", "MAXY"),
#         help="Process only this bounding box (still tiled)",
#     )

#     return parser.parse_args()


# def main():
#     args = parse_args()

#     # -----------------------------
#     # Determine spatial domain
#     # -----------------------------
#     if args.bounds and args.run_global:
#         raise ValueError("Use either --global or --bounds, not both")

#     if args.bounds:
#         domain = dict(
#             minx=args.bounds[0],
#             miny=args.bounds[1],
#             maxx=args.bounds[2],
#             maxy=args.bounds[3],
#         )
#         print(f"Running bounded domain: {domain}")
#     else:
#         domain = GLOBAL_DOMAIN
#         print("Running global domain")

#     output_dir = REPO_ROOT / "results" / "automatic"
#     output_dir.mkdir(parents=True, exist_ok=True)

#     # -----------------------------
#     # Tile processing loop
#     # -----------------------------
#     for tile in generate_tiles(
#         domain["minx"],
#         domain["miny"],
#         domain["maxx"],
#         domain["maxy"],
#         TILE_SIZE,
#     ):
#         print(f"\nProcessing tile {tile}")

#         # --- Resampling atlas & demand ---
#         resampled_output = output_dir / (
#             f"abundance_{tile[0]}_{tile[1]}_{tile[2]}_{tile[3]}_{START_YEAR}_{END_YEAR}.nc"
#         )

#         if resampled_output.exists():
#             print(f"    → Atlas file {resampled_output} exists, skipping.")
#             continue
#         else:
#             try:
#                 print("    → Resampling atlas wind & solar...")
#                 ds_atlas = resample_atlas(tile, PATHS, resolution=REFERENCE_RESOLUTION)
#                 ds_atlas.to_netcdf(resampled_output)
#             except Exception as e:
#                 print(f"    → Error processing atlas for tile {tile}: {e}")
#                 continue


#         # --- ERA5 variability ---
#         variables = {
#             "solar": "ssrd",
#             "wind": "ws100",
#         }

#         for label, variable in variables.items():
#             variability_output = output_dir / (
#                 f"variability_{label}_{tile[0]}_{tile[1]}_{tile[2]}_{tile[3]}_{START_YEAR}_{END_YEAR}.nc"
#             )
#             if variability_output.exists():
#                 print(f"    → Variability file {variability_output} exists, skipping.")
#                 continue
#             else:
#                 try:
#                     ds_variability = run_variability_for_tile(tile, variable)
#                     ds_variability.to_netcdf(variability_output)
#                 except Exception as e:
#                     print(f"    → Error processing variability for tile {tile}: {e}")
#                     continue




# if __name__ == "__main__":
#     main()
